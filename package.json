{
  "name": "ossified-osiris",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
    "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "astro": "astro",
    "deploy": "astro build && npx gh-pages -d dist"
  },

  "dependencies": {
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
